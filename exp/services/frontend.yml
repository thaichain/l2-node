version: '3.9'

services:
  frontend:
    image: ghcr.io/otternode-io/frontend
    pull_policy: always
    restart: always
    container_name: 'frontend'
    env_file:
      -  ../envs/common-frontend.env
